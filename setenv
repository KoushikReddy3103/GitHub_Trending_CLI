if [ -d .venv ];
then
    python -m pip install --upgrade pip setuptools wheel
    python -m pip install -e .
    source .venv/bin/activate
else
    python -m venv .venv
    python -m pip install --upgrade pip setuptools wheel
    python -m pip install -e .
    source .venv/bin/activate
fi